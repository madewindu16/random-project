name: CI/CD Local Deploy

on:
  push:
    branches:
      - main

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build -t random-project .

      - name: Deployment Simulation
        run: echo "Deploy local berhasil"